spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  kafka:
    bootstrap-servers: localhost:9092

shopify:
    api:
        base-url: https://your-test-shopify-store.myshopify.com
        access-token: your_test_access_token